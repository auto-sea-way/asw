# v0.1.0 Release Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship auto-sea-way v0.1.0 as a ready-to-run maritime routing service with pre-built graph on GitHub Releases, slim+full Docker images, and deployment docs.

**Architecture:** Single `workflow_dispatch` release workflow replaces two tag-triggered workflows. It builds binaries, creates a GitHub Release with the graph file, then builds multi-arch Docker images (slim + full). A new `asw healthcheck` CLI subcommand enables Docker health checks in distroless images.

**Tech Stack:** Rust (clap CLI), GitHub Actions (workflow_dispatch, QEMU, buildx), Docker (multi-stage, distroless)

**Spec:** `docs/superpowers/specs/2026-03-16-v010-release-design.md`

---

## Chunk 1: Code Fixes and Healthcheck

### Task 1: Fix fmt and clippy warnings on feat/compact-graph

**Files:**
- Modify: `crates/asw-core/src/graph.rs`

- [ ] **Step 1: Run cargo fmt to identify issues**

Run: `cargo fmt --all -- --check`
Expected: Formatting diff in `crates/asw-core/src/graph.rs`

- [ ] **Step 2: Fix fmt warnings**

In `crates/asw-core/src/graph.rs`, the multi-line `anyhow::bail!()` calls at lines 186-188 and 191-194 need to be collapsed if they exceed the fmt line-length rules. Run:

```bash
cargo fmt --all
```

- [ ] **Step 3: Fix clippy warnings**

Replace `(n + 7) / 8` with `n.div_ceil(8)` in three locations in `crates/asw-core/src/graph.rs`:

Line 118 (in `RoutingGraphBuilder::build`):
```rust
// Before:
let mask_len = (num_nodes as usize + 7) / 8;
// After:
let mask_len = (num_nodes as usize).div_ceil(8);
```

Lines 234-238 (in `RoutingGraph::load`):
```rust
// Before:
graph.passage_mask.len() == (n + 7) / 8,
"passage_mask length {} != expected {}",
graph.passage_mask.len(),
(n + 7) / 8
// After:
graph.passage_mask.len() == n.div_ceil(8),
"passage_mask length {} != expected {}",
graph.passage_mask.len(),
n.div_ceil(8)
```

- [ ] **Step 4: Verify both pass**

Run: `cargo fmt --all -- --check && cargo clippy --workspace -- -D warnings`
Expected: No warnings, exit 0

- [ ] **Step 5: Run tests**

Run: `cargo test --workspace`
Expected: All tests pass

- [ ] **Step 6: Commit**

```bash
git add crates/asw-core/src/graph.rs
git commit -m "fix: resolve fmt and clippy warnings in graph.rs"
```

### Task 2: Add `asw healthcheck` subcommand

The distroless Docker image has no `wget` or `curl`. We need a built-in healthcheck command that hits `http://localhost:$ASW_PORT/ready` and exits 0 (ready) or 1 (not ready).

**Files:**
- Modify: `crates/asw-cli/src/main.rs`

- [ ] **Step 1: Add Healthcheck variant to Commands enum**

In `crates/asw-cli/src/main.rs`, add to the `Commands` enum (after the `Bench` variant):

```rust
    /// Health check: exit 0 if server is ready, 1 otherwise (for Docker HEALTHCHECK)
    Healthcheck {
        /// Server port to check
        #[arg(long, env = "ASW_PORT", default_value = "3000")]
        port: u16,

        /// Server host to check
        #[arg(long, env = "ASW_HOST", default_value = "127.0.0.1")]
        host: String,
    },
```

- [ ] **Step 2: Add match arm in main()**

In `crates/asw-cli/src/main.rs`, add the match arm inside the `match cli.command` block, before the `Commands::Cloud` arm:

```rust
        Commands::Healthcheck { port, host } => {
            let url = format!("http://{}:{}/ready", host, port);
            let resp = reqwest::blocking::get(&url);
            match resp {
                Ok(r) if r.status().is_success() => {
                    std::process::exit(0);
                }
                _ => {
                    std::process::exit(1);
                }
            }
        }
```

- [ ] **Step 3: Verify reqwest dependency exists**

`reqwest` with blocking is already available in `crates/asw-cli/Cargo.toml` (via workspace dependency). No changes needed — just verify it compiles.

- [ ] **Step 4: Build and test manually**

Run: `cargo build --release -p asw-cli`
Expected: Compiles without errors

Test (no server running):
```bash
./target/release/asw healthcheck --port 39999; echo $?
```
Expected: Exit code 1

- [ ] **Step 5: Verify fmt and clippy pass with new code**

Run: `cargo fmt --all -- --check && cargo clippy --workspace -- -D warnings`
Expected: No warnings, exit 0

- [ ] **Step 6: Commit**

```bash
git add crates/asw-cli/src/main.rs
git commit -m "feat: add healthcheck subcommand for Docker HEALTHCHECK"
```

---

## Chunk 2: Dockerfile.full and Release Workflow

### Task 3: Create Dockerfile.full

**Files:**
- Create: `Dockerfile.full`

- [ ] **Step 1: Create Dockerfile.full**

```dockerfile
FROM rust:1.94.0-bookworm AS builder
WORKDIR /src

# Copy manifests only (layer caching)
COPY Cargo.toml Cargo.lock ./
COPY crates/asw-core/Cargo.toml crates/asw-core/Cargo.toml
COPY crates/asw-build/Cargo.toml crates/asw-build/Cargo.toml
COPY crates/asw-serve/Cargo.toml crates/asw-serve/Cargo.toml
COPY crates/asw-cloud/Cargo.toml crates/asw-cloud/Cargo.toml
COPY crates/asw-cli/Cargo.toml crates/asw-cli/Cargo.toml

# Dummy source for dep caching
RUN for crate in asw-core asw-build asw-serve asw-cloud; do \
      mkdir -p crates/$crate/src && echo "" > crates/$crate/src/lib.rs; \
    done && \
    mkdir -p crates/asw-cli/src && echo "fn main() {}" > crates/asw-cli/src/main.rs
RUN cargo build --release -p asw-cli || true

# Real source
COPY crates/ crates/
RUN touch crates/*/src/*.rs && cargo build --release -p asw-cli

FROM gcr.io/distroless/cc-debian12
COPY --from=builder /src/target/release/asw /usr/local/bin/asw
ARG ASW_GRAPH_URL
ADD ${ASW_GRAPH_URL} /data/asw.graph
ENV ASW_GRAPH=/data/asw.graph
ENV ASW_HOST=0.0.0.0
ENV ASW_PORT=3000
EXPOSE 3000
ENTRYPOINT ["asw"]
CMD ["serve"]
```

Note: No `VOLUME /data` — the graph is baked in.

- [ ] **Step 2: Commit**

```bash
git add Dockerfile.full
git commit -m "feat: add Dockerfile.full with baked-in graph for zero-config deployment"
```

### Task 4: Rewrite release.yml as workflow_dispatch

**Files:**
- Modify: `.github/workflows/release.yml`
- Delete: `.github/workflows/docker.yml`

- [ ] **Step 1: Rewrite release.yml**

Replace the entire contents of `.github/workflows/release.yml` with:

```yaml
name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Semantic version (e.g., 0.1.0)"
        required: true
        type: string
      graph_url:
        description: "Public URL to pre-built asw.graph file (e.g., from a draft release)"
        required: true
        type: string

concurrency:
  group: release
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  check:
    uses: ./.github/workflows/ci-check.yml

  build:
    needs: [check]
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            binary: asw-linux-amd64
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            binary: asw-linux-arm64
            cross: true
          - target: x86_64-apple-darwin
            os: macos-13
            binary: asw-darwin-amd64
          - target: aarch64-apple-darwin
            os: macos-latest
            binary: asw-darwin-arm64
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Add target
        run: rustup target add ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-release-${{ matrix.target }}-${{ hashFiles('rust-toolchain.toml') }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-release-${{ matrix.target }}-

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --target ${{ matrix.target }} -p asw-cli

      - name: Build (native)
        if: ${{ !matrix.cross }}
        run: cargo build --release --target ${{ matrix.target }} -p asw-cli

      - name: Rename binary
        run: cp target/${{ matrix.target }}/release/asw ${{ matrix.binary }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}
          path: ${{ matrix.binary }}

  release:
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Guard — must run on main
        run: |
          if [ "${{ github.ref }}" != "refs/heads/main" ]; then
            echo "::error::Release must be triggered from main branch (got ${{ github.ref }})"
            exit 1
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Download graph file
        run: curl -fSL "${{ inputs.graph_url }}" -o asw.graph

      - name: Collect binaries and generate checksums
        run: |
          mkdir -p release
          find artifacts -type f -name 'asw-*' -exec cp {} release/ \;
          cp asw.graph release/
          cd release
          sha256sum asw-* asw.graph > SHA256SUMS

      - name: Create annotated tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ inputs.version }}" -m "Release v${{ inputs.version }}"
          git push origin "v${{ inputs.version }}"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          files: |
            release/asw-*
            release/asw.graph
            release/SHA256SUMS
          generate_release_notes: true

  docker:
    needs: [release]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push slim image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha,scope=slim
          cache-to: type=gha,mode=max,scope=slim

      - name: Build and push full image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.full
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            ASW_GRAPH_URL=https://github.com/${{ github.repository }}/releases/download/v${{ inputs.version }}/asw.graph
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.version }}-full
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-full
          cache-from: type=gha,scope=full
          cache-to: type=gha,mode=max,scope=full
```

- [ ] **Step 2: Delete docker.yml**

```bash
rm .github/workflows/docker.yml
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git rm .github/workflows/docker.yml
git commit -m "feat: unified workflow_dispatch release with Docker multi-arch images"
```

---

## Chunk 3: Deployment Guide and README Updates

### Task 5: Write deployment guide

**Files:**
- Create: `docs/deployment.md`

- [ ] **Step 1: Create docs/deployment.md**

Write the deployment guide with three sections: Docker Compose, Kubernetes, and Bare-metal. Replace `{owner}` and `{repo}` below with the actual GitHub owner and repo name (check with `git remote get-url origin` — the existing README uses `auto-sea-way/asw`).

The file should contain:

**Docker Compose — Full image (zero-config):**
```yaml
services:
  asw:
    image: ghcr.io/{owner}/auto-sea-way:0.1.0-full
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "/usr/local/bin/asw", "healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
```

**Docker Compose — Slim image (with download):**
```yaml
services:
  asw:
    image: ghcr.io/{owner}/auto-sea-way:0.1.0
    ports:
      - "3000:3000"
    environment:
      - ASW_GRAPH_URL=https://github.com/{owner}/{repo}/releases/download/v0.1.0/asw.graph
    volumes:
      - asw-data:/data
    healthcheck:
      test: ["CMD", "/usr/local/bin/asw", "healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 120s
volumes:
  asw-data:
```

**Kubernetes — Deployment + Service** with:
- Readiness probe: `httpGet` on `/ready`, port 3000, `periodSeconds: 5`
- Liveness probe: `httpGet` on `/health`, port 3000, `periodSeconds: 10`
- Full image example (straightforward Deployment)
- Slim image example with PVC for graph persistence
- ClusterIP Service
- Optional Ingress example

**Bare-metal:**
- Download commands using `gh release download` or `curl`
- Run command: `asw serve --graph ./asw.graph --port 3000`
- Systemd unit file example:

```ini
[Unit]
Description=auto-sea-way maritime routing
After=network.target

[Service]
ExecStart=/usr/local/bin/asw serve --graph /var/lib/asw/asw.graph
Restart=on-failure
RestartSec=5
Environment=ASW_PORT=3000
Environment=ASW_HOST=0.0.0.0

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: Commit**

```bash
git add docs/deployment.md
git commit -m "docs: add deployment guide (Docker Compose, Kubernetes, bare-metal)"
```

### Task 6: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Update Full Planet Build section**

Replace the "Full Planet Build" section (lines 79-97) with updated stats from the current build:

```markdown
## Full Planet Build

Built on Hetzner ccx33 (8 dedicated AMD CPUs, 32 GB RAM):

| Metric | Value |
|--------|-------|
| Nodes | 40,397,636 |
| Edges | 305,031,722 |
| Graph file size | 843 MB |
| Connectivity | 96.9% (largest component: 39.1M nodes) |

```bash
asw cloud build --output export/asw.graph
```
```

- [ ] **Step 2: Update Docker section**

Replace the Docker section (lines 19-32) to show both slim and full image usage:

```markdown
## Docker

```bash
# Full image — zero config, graph included (~870 MB)
docker run -p 3000:3000 ghcr.io/{owner}/auto-sea-way:0.1.0-full

# Slim image — auto-download graph on first start (cached in volume)
docker run -e ASW_GRAPH_URL=https://github.com/{owner}/{repo}/releases/download/v0.1.0/asw.graph \
  -v asw-data:/data -p 3000:3000 ghcr.io/{owner}/auto-sea-way:0.1.0

# Slim image — mounted graph file
docker run -v /path/to/asw.graph:/data/asw.graph -p 3000:3000 ghcr.io/{owner}/auto-sea-way:0.1.0
```

See [Deployment Guide](docs/deployment.md) for Docker Compose, Kubernetes, and bare-metal examples.
```

- [ ] **Step 3: Update Known Limitations**

Replace the Known Limitations section (lines 118-121) to add Panama/Kiel canal bugs:

```markdown
## Known Limitations

- **No depth data.** Routing treats all water as navigable — there is no bathymetry or draft-clearance check. This is generally fine for small craft like sailing boats but may route larger vessels through shallow areas.
- **Panama Canal routing.** The Panama Canal passage is not correctly connected, causing routes to go around South America instead. Fix planned for a future release.
- **Kiel Canal routing.** The Kiel Canal passage is not correctly connected, causing routes to go around Denmark instead. Fix planned for a future release.
```

- [ ] **Step 4: Add benchmark section**

Add a "Routing Benchmarks" section after the Full Planet Build section with the 20-route benchmark table. Use the data from `benchmarks/BENCHMARKS.md`.

- [ ] **Step 5: Verify README renders correctly**

Skim the full README for consistency — ensure no stale references to old stats remain.

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: update README with current build stats, Docker images, and known limitations"
```

---

## Chunk 4: Merge and Release

### Task 7: Merge to main and trigger release

- [ ] **Step 1: Verify CI passes locally**

```bash
cargo fmt --all -- --check && cargo clippy --workspace -- -D warnings && cargo test --workspace
```
Expected: All pass, exit 0

- [ ] **Step 2: Commit or stash any uncommitted changes**

Check `git status`. The branch may have modified files (benchmarks, config.rs). Commit them or stash before merging:

```bash
git add -A && git commit -m "chore: update benchmarks and config for ccx33 build"
```

- [ ] **Step 3: Merge feat/compact-graph to main**

```bash
git checkout main
git merge feat/compact-graph --no-ff -m "merge: feat/compact-graph — compact binary format, v0.1.0 release infrastructure"
```

- [ ] **Step 4: Create draft release and upload graph**

```bash
gh release create draft-graph --draft --title "Graph staging" --notes ""
gh release upload draft-graph export/asw.graph
```

- [ ] **Step 5: Get the graph asset URL**

```bash
gh release view draft-graph --json assets --jq '.assets[0].url'
```

Copy the URL — it will be the `graph_url` input.

- [ ] **Step 6: Trigger the release workflow**

```bash
gh workflow run release.yml -f version=0.1.0 -f graph_url=<URL_FROM_STEP_5>
```

- [ ] **Step 7: Monitor workflow**

```bash
gh run list --workflow=release.yml --limit=1
gh run watch <run_id>
```

Expected: All 4 jobs pass (check → build → release → docker)

- [ ] **Step 8: Clean up draft release**

```bash
gh release delete draft-graph --yes --cleanup-tag
```

- [ ] **Step 9: Verify the release**

```bash
gh release view v0.1.0
```

Expected: Release page shows 4 binaries, SHA256SUMS, and asw.graph. Docker images available at ghcr.io with `:0.1.0`, `:latest`, `:0.1.0-full`, `:latest-full` tags.
